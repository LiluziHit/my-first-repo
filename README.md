# my-first-repo

## Goal
Practice repository for AILT9019 AI Literacy II.
Used to learn GitHub, terminal basics, and an AI coding tool.

## Setup
- macOS with Python 3 installed
- GitHub CLI (`gh`) authenticated as `LiluziHit`
- Local folder: `~/Desktop/my-first-repo`

## Successful run
- Created repo via `gh repo create`
- Committed and pushed files using Git
- `hello.txt` contains test output

## Limitations
- Practice only, no real application yet
- No automated tests
- No `.gitignore` set up yet# My First Repo

## Tutorial 2 — Agents, Skills, Context & MCP

- **Context engineering**: designed a clear system prompt with role, source of truth, fallback
- **Skill**: created `my-skill/SKILL.md` (proposal checker) — Pi Agent followed its steps
- **MCP**: configured `.pi/mcp.json` with a weather server
- **Pi Agent**: installed v0.85.1, connected to Kimi, ran `fib.py` (output: 0 1 1 2 3)
